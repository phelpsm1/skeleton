doctype html
html(lang='en')
  head
    style(type="text/css").
      body {
        font-family: Helvetica, Arial, sans-serif;
      }
  body
    p
      <b>#{name} v#{version}</b> (#{revision}) was deployed to <b>#{env}</b> at <b>#{at}</b> by <b>#{by}</b> from #{from}.